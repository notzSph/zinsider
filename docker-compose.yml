services:
  zinsider:
    build: .
    image: zinsider:latest
    env_file:
      - .env
    environment:
      STATE_DIR: /app/state
    volumes:
      - state:/app/state
    restart: "no"

volumes:
  state: